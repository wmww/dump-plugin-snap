name: dump-snap
version: git
summary: A test of the dump plugin
description: A test of the dump plugin
confinement: strict
grade: devel
base: core18

apps:
  dump-snap:
    command: ./say_hello.sh

parts:
  files:
    plugin: dump
    source: files
